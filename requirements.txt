# ─────────────────────────────────────────────
# Guided Fusion — requirements.txt
# ─────────────────────────────────────────────
# Install with:
#   uv pip install -r requirements.txt
#
# NOTE: torch and torchvision are installed via
# pyproject.toml with the CUDA wheel directly.
# Do NOT install torch from this file.
# ─────────────────────────────────────────────

numpy<2
opencv-python>=4.9.0
Pillow>=10.2.0
tqdm>=4.66.0
matplotlib>=3.8.0
scikit-image>=0.22.0
tensorboard>=2.16.0
einops>=0.7.0

# Dev tools (optional)
ruff>=0.3.0
mypy>=1.9.0
pytest>=8.1.0
